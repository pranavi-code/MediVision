import React from "react";
import Chat from "./Chat";

// UploadChat kept for compatibility with your original layout.
// It simply reuses the Chat page so you can keep whichever route you prefer.

const UploadChatPage: React.FC = () => {
  return <Chat />;
};

export default UploadChatPage;
